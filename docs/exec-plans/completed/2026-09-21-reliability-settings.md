# 稳定性、性能与设置体验优化

**Status:** completed
**Created:** 2026-09-21
**Updated:** 2026-09-21

## 任务契约

- 模式：implementation；用户授权检查后修复、性能优化及设置 UI 改进，保留已有功能。
- 允许路径：`Easydict/` 产品源码及必要资源、`EasydictTests/` 回归测试、本文及对应历史。
- 外部服务：无写入授权；不推送、不修改用户配置或真实剪贴板历史。
- 初始 HEAD：`77e6ab941a64d2d40677bb5a34e29b2ed6ee521e`。
- 初始 staged/unstaged/conflicts 为空；untracked `build2/` 保留。
- Mutation Gate: PASS。授权以最新明确的“全面修复”“自由修改”为准。
- 自动提交：eligible，验证完成后创建一次本地提交（提交标识见 Git 历史）；必要验证失败时不提交。
- 验收：有证据的 bug 修复，原有设置项和数据兼容，格式、构建、行为验证，明确未验证范围。

## 里程碑

- [x] 阅读仓库规则、确认基线与范围。
- [x] 检查核心流程，修复数据一致性和异步会话问题。
- [x] 优化设置布局、导航和异步状态反馈。
- [x] 完成独立回归测试、格式检查与 Xcode 验证。
- [x] 复核 diff，归档计划、记录历史并按资格本地提交。

## 风险与决策

- 剪贴板持久化修复通过临时数据库和故障注入验证，避免接触真实用户数据。
- 截图翻译保留服务顺序与串行调用，防止共享服务对象的并发状态竞争。
- 设置保留现有本地化 key 和全部选项；使用 macOS 13 兼容的原生组件。
- 产品实现与测试按仓库要求由不同 Agent 负责；Xcode 构建由主 Agent 串行执行。
- 全面检查不等于证明不存在所有缺陷；第三方 API、系统权限和真实交互边界如实记录。

## 验证记录

- `xcodebuild build-for-testing`：通过（Debug、macOS arm64、ad-hoc 签名）。
- `xcodebuild test`：8 个套件、79 个测试通过；覆盖 ClipboardStore、TaskTimeout、ScreenshotDockLayout、ThrottleGate、MarkdownRenderer、中英文 OCR 文本处理与标点。
- `xcodebuild test-without-building`：AppleScript 的 2 个调用方套件通过，4 项执行通过、1 项按仓库既有规则禁用。
- 新增剪贴板测试 7 个场景：空白过滤、文本及图片插入回滚、复用图片文件、三类删除失败的数据与文件保留；超时工具新增父取消和预取消 2 项测试。
- 变更 Swift 文件 `swiftformat --lint` 与 `git diff --check`：通过。
- 完整构建包含仓库 SwiftLint，仍有原有长文件、命名、行宽及第三方并发警告；不修改无关代码来清理警告。
- 截图保存格式 Picker 最终复核：只展示系统可编码格式，已有不支持格式在 UI 中显示与编码器一致的 PNG 回退，不改写用户存储值；最终增量 `xcodebuild build` 通过。
- 原生 UI 工具连续返回 `timeoutReached`，无法取得设置窗口 AX 树或截图。调试进程启动成功，但不计为视觉验收；已仅关闭本次启动的调试进程，保留原运行应用。
- 未验证：真实服务商凭据及联网翻译、屏幕/辅助功能权限操作、所有 macOS 版本与显示器组合、UI 视觉效果。未做量化性能基准，不宣称百分比提升。

## 实际变更

- 剪贴板：操作级锁覆盖事务，去重失败回滚、先提交删除再清理图片、重用文件保留、查询错误显式上抛、空白搜索规范化。文本去重去掉一次无用 SELECT，删除和插入合并为一次事务。
- 截图翻译：取消后的错误处理不得清空新会话状态；读音返回校验会话；同一轮主队列合并重复布局请求。
- 公共超时：父任务取消立即结束等待，取消两个内部任务，保留成功、超时和原错误传播语义。
- 设置：原生键盘导航、彩色图标、统一标题与窗口尺寸、屏幕范围约束；存储统计跟随页面取消；保存目录首次加载、作用域访问释放、格式可用性处理；服务说明预留空间不覆盖列表。


## 交付

- 原生 UI 截图验证受工具超时限制，产品代码与相关自动化验证已完成；不声称所有功能已完成端到端验证。
- 本次提交只包含列明的产品、测试、计划及历史文件，保留原始 `build2/`；不推送。
